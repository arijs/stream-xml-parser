
export default {
	area:true,// clickable area inside an image map.
	base:true,// new root or base path for relative paths.
	basefont:true,
	bgsound:true,
	br:true,// single line break in a paragraph.
	col:true,
	command:true,
	embed:true,// embeds things like flash.
	frame:true,
	hr:true,// change in type of content.
	iframe:true,// frame inside the page.
	image:true,
	img:true,// image.
	input:true,// input field.
	isindex:true,
	keygen:true,
	link:true,// reference an external resource.
	menuitem:true,
	meta:true,// metadata about the page.
	nextid:true,
	param:true,// parameter for an object.
	source:true,// include media for video or audio.
	track:true,// text track for video or audio.
	wbr:true// word break opportunity
};
